import React from 'react';
import classNames from 'classnames';

const Header = () => {
  return (
    <header
      className={classNames(
        'h-14 w-full bg-background-normal pr-7 pl-48'
      )}></header>
  );
};

export default Header;
